import { HasWebinarIframeLinkPipe } from './has-webinar-iframe-link.pipe';

describe('HasWebinarIframeLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new HasWebinarIframeLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
